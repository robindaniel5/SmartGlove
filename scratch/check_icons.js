import * as Lucide from 'lucide-react';
console.log('Available icons:', Object.keys(Lucide).filter(k => k.toLowerCase().includes('git') || k.toLowerCase().includes('link') || k.toLowerCase().includes('inst')));
